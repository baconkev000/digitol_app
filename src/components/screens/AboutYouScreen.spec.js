// import {render, screen, fireEvent} from '@testing-library/react-native';
// const {default: PhoneNumberScreen} = require('./PhoneNumberScreen');

// test('given empty PhoneNumberScreen, user can input phone number', () => {
//     const phoneNumberValid = '8017550777';
//     const phoneNumberInvalid = '801';
//     const mockFn = jest.fn();

//     render(<PhoneNumberScreen />);
//     const numberInput = screen.getByPlaceholderText('Phone Number');

//     fireEvent.changeText(numberInput, phoneNumberValid)
//     fireEvent.press(screen.getByRole('button'));
// });
